import React from 'react';
import { Link } from 'react-router-dom';

const Detect = () => {
  return (
    <div>
      <h1>Detect</h1>
      <p>Here is some content for the Detect page.</p>
      <Link to="/">Go back to home</Link>
    </div>
  );
}

export default Detect;
